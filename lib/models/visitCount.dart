class VisitCounts {
  final int plannedVisits;
  final int unplannedVisits;
  final int completedVisits;
  final int uncompletedVisits;
  final int totalVis;

  VisitCounts({
    required this.plannedVisits,
    required this.unplannedVisits,
    required this.completedVisits,
    required this.uncompletedVisits,
    required this.totalVis,
  });
}